{
  "name": "speed-percentile",
  "version": "1.0.0",
  "description": "Utility to compute speed percentile from sparse histogram hash.",
  "license": "ISC",
  "main": "index.js",
  "repository": "https://github.com/mapbox/speed-percentile/",
  "engines": {
    "node": "4.2.2"
  },
  "scripts": {
    "test": "npm run lint; tap -R spec test/basic.js",
    "lint": "eslint ./*.js ./test/*.js"
  },
  "dependencies": {

  },
  "devDependencies": {
    "eslint": "^2.11.1",
    "tap": "^5.7.2"
  },
  "eslintConfig": {
    "env": {
      "node": true,
      "es6": false
    },
    "rules": {
      "semi": 2
    }
  }
}
